{
  "name": "op3-root",
  "version": "0.1.0",
  "description": "",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "version:changeset": "changeset",
    "version:bump": "changeset version",
    "version:release": "pnpm build && pnpm --filter @op3/cli inject-deps && changeset publish"
  },
  "keywords": [],
  "author": "apisurf.dev",
  "license": "ISC",
  "packageManager": "pnpm@10.25.0",
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "turbo": "^2.5.6"
  }
}
