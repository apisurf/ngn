{
  "name": "ngn-root",
  "version": "0.1.0",
  "description": "",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "version:bump": "./distribution/with-github-token.sh sh -c 'changeset && changeset version'",
    "version:release": "./distribution/with-github-token.sh ./distribution/release.sh"
  },
  "keywords": [],
  "author": "apisurf.dev",
  "license": "ISC",
  "packageManager": "pnpm@10.28.0",
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "turbo": "^2.5.6"
  }
}
